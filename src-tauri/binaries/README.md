# FFmpeg nativo

`ffmpeg-x86_64-pc-windows-msvc.exe` es el FFmpeg estatico (build "essentials" de gyan.dev, 9.0)
que usa el render. No se sube a git por su tamano (~100 MB): copiarlo aca antes de compilar.
Tauri lo instala al lado de Flowuana.exe como `ffmpeg.exe`.
